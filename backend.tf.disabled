terraform {
  backend "s3" {
    bucket         = "vita-terraform-state-dev"
    key            = "dev/terraform.tfstate"
    region         = "us-east-1"
    dynamodb_table = "vita-terraform-locks-dev"
    encrypt        = true
  }
}